{
  "name": "github-stats-card",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate.mjs",
    "check": "node --check scripts/generate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
